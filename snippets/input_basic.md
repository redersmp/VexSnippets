# Read data from multiple inputs

#### Read point position from second input

```
vector pos = point(1, "P", @ptnum);
```

#### Read point colour from third input

```
@Cd = point(2, "Cd", @ptnum);
```